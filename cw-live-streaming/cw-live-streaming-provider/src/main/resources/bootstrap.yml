spring:
  application:
    name: cw-live-streaming-provider
  cloud:
    nacos:
      server-addr:
          127.0.0.1:8848
      config:
        namespace: cw-live-streaming
      discovery:
        namespace: cw‑live‑streaming